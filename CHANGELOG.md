# 更新日志

## v2026w0911d
- 📖 **图鉴 eternal loading**：渲染失败曾被吞致占位永不替换＋atlasBox 只判空不判`加载中`；改渲染失败抛外层统一重试、有缓存才刷旧、无缓存直报重试。
- 🔀 **通道切换无效**：后端 proxy 优先＋body/query/dict 全收＋写后读回；前端验 `nc===c` 才报成功；check 等候加通道隔离＋无键旧缓存一律 miss。

## v2026w0911c
- 🧹 **退群清理假成功**：前端调错 `users/export`（恒 `ok` 报“清理0人”），改调 `users/clean_left`。
- 🔒 **降级双收费**：赌博/转账主路径异常后未回滚即降级重试，加 `rollback`（redpack 同模式已修）。

## v2026w0911b
- 🧩 **前后端版本互验**：构建注入 `FRONTEND_VER`，关于页前后端对照＋失配红字（pull＋完全重启指引）；
  index.html script 缓存击穿标记（构建自动同步，verify 门覆盖）。
- 🔀 **通道切换说实话**：后端无回执即报错，不再乐观谎报；商城盒失败兜底。

## v2026w0911a
- 🔢 **快照版本制**：beta 改 `年份w月日序号`（`2026w0911a`，同日 a→b 延续，跨日归 a），正式版仍走 semver；
  版本号纪元算法兼容快照（快照恒大于 semver）；聊天 `版本` 回复走单源动态值。
- 🔀 **更新通道**：关于页加 通道显示（正式版/BETA版）＋可切换＋检测更新按钮；BETA 查 xbtest 快照仓，
  正式查官方仓；通道记忆进 recall，检测按通道隔离缓存。
- 📖 **图鉴防卡死**：读超时收口＋失败可重试＋导入加固（BOM/空文件）。

## v0.7.45-beta
- 🔇 **全员静默**：禁用指令/关闭系统/维护无@时全员零回复（含超管），WebUI 为唯一控制面；
- 🛠️ **群级维修**：群内`开启维护`只管本群，私聊管全局，`查看维护`双显，群聊开关 Tab 加维修列；
- 📊 **当前数值**：超管精确指令输出 12 项核心数值＋预设/自定义判定；
- 👍 **赞我**：每日点赞/点赞合并为精确触发词（schema 键同步迁移）；
- 🟡 **新装默认休闲**：无持久配置＋空库真新装自动套 casual，老用户绝不覆盖；
- 🔧 **潜伏修复**：router 合并丢 `global` 致指令索引永空、`db.init` 调不存在的 `set_config`（AstrBot 下发配置静默丢失）；
- 🔢 **版本号**：顺延并加 beta 后缀（`0.7.45-beta`），对齐点全过 verify。

## v0.7.44
- 🎮 **娱乐续命**：答题/字谜/急转弯/猜数/二四点过期改 `max(start,last_time)`，持续作答不再被误结束；加入同口径。
- 🏦 **打劫/红包原子**：银行打劫失败重读钳制再试，个人打劫走原子划转，红包终扣重读钳制，堵并发印钱超发；`merge_from` 异常回滚+关库。
- ⚡ **接口异步化**：`spirits/get`/`gacha/weapons` 进线程池，不堵事件循环；超管检查更新 60s 缓存防占满 12 线程池。
- 🎯 **路由最长命中**：禁“签到”不再误杀“签到系统”，权限同理；自定义触发词去首尾空格。
- 🗄️ **快照/池收敛**：平衡快照 fallback 上限 19→5；非覆盖上传同 stem 任意扩展名判重，走统一写口防孤儿。
- 🗑️ **精简规整**：删 `superadmin._j`/`main._backup_base/_safe_backup` 死码，`_err/_raw_file` 收口 helpers，清 `__pycache__`。

## v0.7.43
- 🛡️ **迁移 once 语义**：sidecar 已存在时重启只逐出内存，绝不回写（种子/旧参数不再覆盖用户商城定制）。

## v0.7.42
- 🧪 **探针效果断言**：157→237 条（8系统分文件），钱包/归属/冷却/背包/坐骑/帮派/会话全量状态断言替代纯文本；拼音背包/`active` 串表等 6 类摆设探针根治；全过且连跑确定。
- 📁 **测试隔离**：`core/test_harness.py`+`dispatch.py` 迁 `selftest/`（探针/执行/断言分文件），`main` 懒加载调用；`超管列表` 死代码删除。
- 🗄️ **商城图鉴独立存储**：`shop.json`/`atlas.json` sidecar，不进 config.json / DB 镜像 / 快照；启动自动迁移；旧快照恢复自动收编。
- 🛡️ **保留数双修**：镜像回填仅缺键生效；空配置拒落盘+原子写。

## v0.7.41
- 🧪 **测试testxb根治**：删真实群/QQ回退（不再污染真号真群）；逐条✅❌+通过率；背包中文key+大师球/领养标记（成功分支真跑通）；接龙第三人测拦截；二四点真验可解性；红包/会话/奴隶档全清；执行进线程池+转发8秒熔断+机器人UIN。

## v0.7.40
- 🛡️ **保留数防丢**：`wd_cfg_restore` 仅缺键/空值回填（合法 30 不再被旧镜像盖掉）；`save_config` 拒空写+原子落盘。
- ⚡ **管理接口异步化**：武器池 8 接口/图片 6 接口/群组 2 接口/`config/save`/旧库导入重活进线程池；`stats` 锁内只取数。
- 🗑️ **精简**：删 `cmd_force_take`/`save_all`/`_cfg_get`/`_sync_weapon_shop`/同步读文件/`en_to_cn`/`set_log_dir`/`warning`/`DEFAULT_CFG`、前端死绑定（moreBtn/分类箭头/双通道导出）与废弃 CSS。
- ⚡ **WebUI**：导出成功不再弹手动窗、备份搜索本地过滤、WebDAV 成功回执去重、CSS 变量正名、表格行懒渲染扩展。
- 🐞 **修**：应急菜单版本、快照毫秒防撞、保存校验覆盖保留数/间隔、群刷新死按钮、精灵 `bag` 全量下发改计数。

## v0.7.39
- ⚡ **导出双发收敛**：`exportAllUsers`/`exportImages` 手写 `apiGet→apiPost` 改单次 `callApi(GET)`。
- 🗑️ **死按钮删除**：`btnBackupExport` 无 DOM（仅 `btnBackupExportSel` 存在），整段删。
- 🧹 **娱乐清场补齐**：答题/猜数/二四点胜局与超时同步清 `last_time`，KV 不留僵尸。

## v0.7.38
- 🐞 **纪元阈值修正**：`updater` 0.69 误判旧纪元，收敛至 0.68。
- 🎮 **娱乐自愈补齐**：答题/字谜/急转弯/猜数/二四点开局埋 `last_time`、作答续命、退出清零；持续答题不再被误判闲置顶号。
- ⚡ **主循环限流**：`run_in_executor` 换定长 12 线程池，突发千群可控。
- 🗄️ **读放大收敛**：`slave/users` 仅修复脏身价才落盘，无修复零写。
- 🗑️ **模拟器死绑定删除**：`index.html` 无 DOM、`sendSimulatorCommand` 未定义、双处重复绑定，删干净+空投防重。
- ⚡ **WebUI 提速**：日志单次 `callApi`、7 搜索框 200ms 防抖、表格行 `content-visibility`、武器缩略 `lazy`。
- 🧪 **测试对齐现状**：`bridge.download` 改 `triggerDownload` 链、`_dispatch` 窗口 2000→8000、群管鉴权白名单 `platform`。

## v0.7.37
- 🗑️ **总览改名+去刷新按钮**：全群数据总览；KPI 随页签加载，手动刷新按钮与其重复绑定一并删除。
- 🗑️ **删补偿指令**：与银行转账重复，路由/函数/三表/手配/菜单/预设死键全清（打赏上限/间隔保留可调）。
- 🗑️ **图鉴去坐骑**：坐骑只在商城管理，图鉴保留宝物/精灵。
- 🗄️ **快照防堆积**：pre_balance 绕开 5 份上限攒 20 份且从不删行；统一走索引上限+孤儿行清理+latest 扶正（旧库一次自愈）。
- 📥 **商城导出重做**：坐骑+武器属性/顺序+武器清单+精灵道具全量包，导入兼容旧格式，缺图跳过提示。
- 🛒 **商城大类排序**：坐骑/武器池/道具三栏 ↑↓ 调序持久化。
- ⬆️ **回到顶部**：全页签通用小浮钮（半屏出现，兼顾图鉴/日志滚动区）。
- ⚡ **滑动与总览提速**：头栏去 backdrop-blur（不透明底纯耗电）、长列表 content-visibility、砍未使用的精灵聚合子查询。

## v0.7.36
- 🗑️ **图鉴去武器页签**：武器只在商城抽奖武器池管理，图鉴保留宝物/坐骑/精灵；武器 ↑↓ 排序搬到商城池行内。
- 🎯 **指令数值去乱配**：删 `查询`/`我的信息`→货币名称 手配，兜底本就无关联，现显示无关联可调数值。

## v0.7.35
- ✎ **精灵改名行内化**：与坐骑同款，名字直接点改，回车/失焦生效，空名重名自动回滚，地图掉落与进化指向同步，点保存精灵生效。

## v0.7.34
- 🗑️ **删金字塔与24h走势**：24h曲线本就是用户数套公式的装饰假数据；前端两块+渲染函数+后端阶层SQL/曲线一并清除，KPI大屏保留。
- 🎯 **漂移抽检12键**：剔除出厂混合态的抽武器花费（schema/引擎/标准=1988、休闲=1000，更新即回填致永恒1项误报），其余12键三档全异检测力无损。
- 📥 **导出重做**：商城/图鉴/备份导出改走统一导出通道（自动下载+弹窗手动保存/复制兜底），删死包装 `downloadBlob`。

## v0.7.33
- 🧹 **清空精灵地图**：一键清地图留精灵（进未上架区），二次确认+即时硬清理，方便从零自定义。
- 🔀 **三区排序**：坐骑/武器/精灵地图 ↑↓ 调序；坐骑地图走现有保存，武器顺序存 `商城图鉴.weapon_order` 即时持久、无序新文件按名追加。
- ✎ **精灵改名**：主键+全地图掉落+进化指向同步更名，点保存精灵生效。
- 🐛 顺带修图鉴宝物/坐骑删除后不刷新。

## v0.7.32
- 🧧 **红包降级超发修复**：降级路径发钱不同步扣余量，多人可超总额（实测超发 10%）。补余量扣减，总额守恒。
- 🗑️ **自定义删除按钮复活**：id 写错导致永不显示，一行修复。
- 🎚️ **5 键补回配置面**：买入身价上涨/赎身花费倍率/祈福间隔/释放间隔/折磨成功率（引擎在读、预设有写，WebUI 不可见），schema+种子补齐，默认值=种子（零行为变化）。
- ⛓️ **进监狱次数可配**：硬编码 5 次/天改为配置键，三档全 8 次。
- 🧹 **小清理**：查询静默全角空白、空图鉴保存语义、关于页版本占位、超管清空联动公会失效、4 处死引用/防御、文档行数。

## v0.7.31
- 🎯 **漂移误报根治**：出厂种子实为休闲数值但检测只比标准档，新装即报12项偏离。改逐档推断（命中即未偏离）+ 缺键豁免（空=未自定义），零数值变化；弹窗预选真实档，徽标自动纠正。
- ⚡ **指令页提速**：双接口并行 + `#cmdList` 事件委托一次绑定（修全部展开/收起重复绑定）+ 保存后保持展开态。
- 🛒 **商城布局**：保存/恢复上移与添加同栏；坐骑独立小保存；顶部保存升级为全部保存（坐骑/武器属性/精灵道具各存各的）；收服率去“大师球必中”后缀。
- 📦 **安装包精简**：文档（AIINFO/AIREADME/aiall/CHANGELOG/README）与 scripts/ 不进 zip；删空 t1/ 目录。

## v0.7.30
- ⚡ **重接口异步化**：奴隶/精灵画像与身价校准、日志查看/清空/导出走 `asyncio.to_thread`，大群点 Tab 不再堵事件循环；其余毫秒级池文件接口保持同步零漂移。
- 📜 **日志链路减负**：轮询内容无变化跳过 500 行 DOM 重建（手动刷新仍实时渲染）。
- 🔄 **更新检测收敛**：镜像 7→3、单点超时 3→2 秒（最坏 25→10 秒，低于前端 20 秒熔断）；成功缓存 10 秒→5 分钟、失败缓存 1 分钟；并发在途共享，防惊群。
- 🎯 **打劫取候选有界化**：`ORDER BY RANDOM() LIMIT 8` + 自过滤，数学上仍均匀，千人群不再全表拉取。
- 🏯 **帮派缓存 5→15 秒**：成员变更仍主动失效，战力快照 stale 可接受。

## v0.7.29
- 🗑️ **删9查询转发**：查询更新/版本/维护不再越权转发超管（与全静默红线冲突），查询坐骑/精灵/帮派/冒险/地图/菜单静默放行；指令页215→206，schema 928→901项。
- 🧹 **必要配置新增全部设置恢复默认**：一键覆盖数值/开关类设置，白名单排除备份配置（含WebDAV/自动备份开关/间隔/保留数）、商城图鉴、精灵图鉴、自定义指令、群组开关；不动用户数据；二次确认+自动快照可回滚。
- ✨ **商城三栏标题精简**：只留“X商城 — N 件”，去英文键名与介绍后缀。
- 🛠️ **安全与精简**：空投失败改 `_safe_rollback`；发红包删重复查询+异常先回滚防重复扣；空结果不再双发POST；删影子 `filterUsers`/`get_latest_bot`；修 `badge-warning` 死类名；去 `chk-wrap` 空类；原生 `confirm/prompt` 回退改静默取消。

## v0.7.28
- 🗄️ **备份防重收敛**：每次用共享时钟（DB 镜像+目录最新）刷新节拍，多进程/重载不再交错打双份。
- 🎯 **数值精准匹配**：指令编辑器按二字关键词匹配本系统数值，对得上才显示，无关的不再出现、无关的如实显示无关联。
- 🧹 删备份页防重提示；商城三栏添加按钮置顶。
- ⚡ **商城/图鉴提速**：砍掉写-only 的 gacha/weapons 请求；加载链去双重渲染；精灵卡事件委托（顺带修好未上架区按钮无响应）。

## v0.7.27
- 🔒 **超管全锁**：版本/检查更新等不再对普通用户开放，非超管一律静默；超管去17个别名模糊词（一令一名）。
- 🎚️ **每指令超管权限开关**：新增指令权限配置节（215键，30键默认超管），指令编辑器可逐条切换，🔒徽标可见。
- 🎚️ **每指令数值全覆盖**：编辑器按所属系统自动列出全部可调数值（215条指令×各自系统奖励/惩罚/概率/价格/间隔）。
- 🧹 **采集去噪**：单字选项（一/二/三）与否定守卫前缀（我要/自我/开始/加入/退出）不再进指令表；补种子遗漏3键。

## v0.7.26
- 🎯 **平衡弹窗可关**：内容区加滚动（小屏不再顶掉按钮）；✕/背景可关；确认失败可重试（之前失败一次就死键）。
- 🧹 **指令去空格重**：查询坐骑/查询 坐骑等 7 组空格变体合并为无空格规范键（234→228），开关/回复/引擎匹配空格无关，旧空格配置照样生效，自定义触发词保持精确。
- 🎚️ **必要配置放行数值节**：设置/费用/间隔/概率/祈福/签到/抽奖/银行/娱乐/精灵/坐骑/帮派/冒险等奖励惩罚概率节全部可在必要配置页直接调整（之前仅 5 个基础节可见）。
- 🐴 坐骑恢复默认补 ↩️ emoji 对齐。

## v0.7.25
- 🗂️ **主入口精简**：`main.py` 68 个 Web API 注册收口为 `_XB_API_ROUTES`/`_XB_WEBDAV_ROUTES` 表驱动循环；60 个 `page_*` 薄包装收口为 `_call_api` 统一委托（双通道导入+参数模式+归一 `_err`），`main.py` 1201→975 行，路由/方法/错误语义经脚本逐项比对等价。

## v0.7.24
- 🛡️ **全量审计修复**：主循环超管列表转线程；大屏/排行/群组/用户/备份/图片/校准等耗时API转`asyncio.to_thread`；恢复清KV缓存；导出排密钥；存储加回滚；武器商城停写；删死函数；补系统开关节；坐骑恢复统一即时硬清；上传收口base64。
- 🗂️ **精简**：用户/排行去逐行fetch_card；shopRide镜像只读；工坊残留文案清理。

## v0.7.23
- 🧬 **精灵保存合并**：保存地图+保存属性合并为保存精灵，一次存maps+spirits，根治只存一半丢形象图；恢复地图/恢复属性合并为恢复默认（二次确认+硬清理）。
- 🖼️ **精灵浏览+添加弹窗**：属性卡加浏览图片（thumb+lightbox）；添加精灵改弹窗可设等级/六维/属性/进化/形象图（内外上传+浏览+精灵目录）。
- 🎒 **道具类型化**：类型改下拉（精灵球/等级/HP/攻击/防御/特攻/特防/进化）+效果说明（球=收服率%，大师球100必中）；添加道具改弹窗选类型。
- 🐴 **坐骑补齐**：浏览改浏览图片；添加坐骑加外置上传+浏览；恢复默认加二次确认并用内置10项；顶部保存只写ride_shop去串写。
- 🗂️ **目录隔离**：精灵→data/img/spirits，坐骑→data/img/rides，武器→data/img/gacha/稀有度，内置选图默认进各自目录。

## v0.7.22
- 🏷️ **平衡徽标真实化**：两处徽标之前硬编码显示休闲，现按 `_active_balance_mode` 真实刷新；徽标可点击直达平衡弹窗。
- 🔍 **漂移检测**：新增 `config/balance_state` 接口，抽检13个跨系统签名键，数值与标记档不符时徽标置灰“已偏离n项”，弹窗内点名具体键。

## v0.7.21
- 📤 **上传 nofile/postMessage 根治**：iframe 桥 `postMessage` 无法克隆 `FormData`，所有上传（池/坐骑/精灵/根目录）改 base64 JSON 直传，后端双受理；坐骑上传改用回执路径绑定。
- 🗂️ **图片目录英文化**：`data/gacha_img`→`data/img/gacha`、`data/img/坐骑图标`→`data/img/rides`；引擎新旧目录双读（持久+内置），已存配置路径别名兼容；自愈播种仅全新时一次，不再覆盖用户改池；上传/诊断/校验同步新路径。
- 🛡️ **保留数防回滚**：新增三源投票（持久文件+DB镜像一致则覆盖内存），专治重启/重载把保留数等滚回旧值；sidecar 加注“此文件为防重文件，无需删除”。
- 🖼️ **行级对齐**：坐骑行去来源字、缩略图改浏览键；池行默认参照行保留。

## v0.7.20
- 🖼️ **图片链路对齐**：新增通用 `images/thumb` 单张预览接口；坐骑行缩略图（展开懒加载，点击放大），无自定义图自动匹配坐骑图标；精灵属性卡加外置选图（本地上传到 `data/img/精灵`）/内置选图（服务器文件）；武器池去掉预览按钮（点缩略图直接放大）。
- 🐛 **坐骑上传 nofile 根治**：弃用桥接 `upload`，改与武器池相同的表单直传（`images/upload?dir=`），绑定路径用后端回执不再硬拼；选图统一改名外置选图，顺序外置→内置→改名/删除全端一致。
- 📱 **手机平板适配**：`≤768px` 编辑行单列全宽、输入框 100% 不溢出、按钮区换行、地图头换行。

## v0.7.19
- 🎰 **添加武器弹窗**：武器名/稀有度/攻击加成/描述一次填完，图片二选一（外置本地上传 / 内置服务器选图）；上传接口支持 `name` 指定文件名；替换接口支持新建（`replace_path` + `create`）；属性接口改合并写，单件保存不再清空其他。
- 📌 **池行默认参照**：每行显示默认加成/描述/星级基础表。
- 🐴 **坐骑同款**：列表无自定义图时自动匹配坐骑图标并标注来源；添加坐骑窗加内置选图按钮。
- 🔒 **备份防重提示**：`backups/list` 下发 sidecar 可读标记，备份时间后显示 🔒 防重保护中（悬停看说明）。
- 🐛 **宝物通用效果补齐**：`TREASURE_EFFECT_GENERIC` 常量缺失导致非专属宝物效果为空，现回退 `T_COMMON_EFFECT`（镇宅纳福）。

## v0.7.18
- 🐛 **抽武器出金丢档**：`slave.handle` 只给字符串回复落盘，带图元组（抽武器/坐骑相关）直接返回，重启丢数据还照扣钱；现统一落盘。
- 🐛 **宝物系统全死**：引擎读 `设置.treasure`，配置/图鉴写 `设置.宝物`，两边对不上导致学习永不掉宝、菜单无宝；新增 `_treasure_names()` 双键合并，掉落/菜单/升阶/详情全活，自定义效果经 `treasure_effects` 实际生效。
- 🔑 **WebDAV 密钥分存**：地址/账号/密码移入数据目录独立 `webdav_secret.json`（`ST.cfg` 运行时透明叠加，零改 `webdav.py`）；`config/save` 分流写入、`config/get` 回填显示（密码恒空，留空保持不变）；DB 镜像/快照/`config.json` 只留空；启动一次性迁移旧密钥并清空历史镜像位（历史备份文件不受影响）。
- 🎰 **池行对齐坐骑商城**：SSR/SR/R 分区默认收起；每行攻击加成/描述可配（`weapon_attrs`，加成计入战力、描述进详情，旧 `weapon_shop` 残留只读兼容）；内置选图（服务器文件复制）/外置选图（本地上传覆盖）；图片分区展开时懒加载；商城改单列全宽，删重复 🛒 标题。
- 🧩 **总览每 tab 保存恢复**：武器/宝物/坐骑/精灵各一对按钮；恢复默认均为二次确认后硬清理即时生效（旧数据不保留，数据库备份可回滚）；图鉴多余提示句删除。

## v0.7.17
- 🎰 **抽奖池直管补完**：列表不再批量下发缩略图（卡顿根源），改为按需单张预览；每件加换图按钮（覆盖同名旧文件）；`weapons/pool/img` 新接口。
- 🐛 **搜后保存丢地图根治**：`saveSpirits` 曾从搜索过滤后的 DOM 收集，非匹配地图会被删光；现保存/恢复全部按系统走内存状态（输入即时写回），视图过滤不再影响数据。
- 🧩 **保存恢复按系统拆分**：总览 ✨ 精灵头设保存地图/保存属性/恢复地图/恢复属性，道具在商城页保存/恢复；删除上方重复地图编辑区（搜索框/列表/切换按钮），总览为唯一入口；导出固定干净 `{spirits,maps,shop}`，与导入同一套。

## v0.7.16
- 🎰 **武器商城改抽奖武器池直管**：删除武器商城概念，商城/总览改为文件级可编辑池（改名/改稀有度/删除/上传即时生效，写生效目录）；`gacha` 抽中不再写 `weapon_shop`，三档预设移除 `weapon_shop`（旧数据仅展示兼容）；新增 `weapons/pool/*` 5 接口。
- 📖 **总览精灵可编辑**：✨ 精灵页签改用与精灵地图同款可编辑卡片（展开/改等级掉落/属性/增删），输入即时写回 SPIRIT，结构操作双视图同步，保存走原保存图鉴；上方地图编辑区默认收起（留 🗺 地图编辑按钮）。
- 🛡️ **恢复默认按页隔离**：必要配置只重置本页已渲染节 + 二次确认；宝物名/效果与坐骑改动在总览内即时保存（只写各自范围）；商城保存只写坐骑，图鉴保存只写图鉴。
- 🔇 **回执精简**：商城/图鉴/配置/指令保存与恢复默认只说结果，不再 dump JSON。

## v0.7.15
- 📖 **总览第四页签**：精灵地图作为 ✨ 精灵页签收进全类目总览（只读，编辑回上方图鉴），移除下方独立框。
- 🐛 **未上架精灵展开修复**：默认收起可点击展开（补 `open` 类）；保存收集跳过 `__orphans__` 虚拟分区，不再写脏地图。

## v0.7.14
- 🛒 **武器商城对齐坐骑**：可改价/改攻击/描述/绑图（选图 + 内置选图），`_weapon_img_path` 优先配置图回退抽奖池。
- 🔧 **维护提示精简**：默认维护信息改为 `🚧 维护中`。
- 📖 **图鉴总览扩展**：全类目下新增精灵系统（精灵地图）独立区；宝物支持自定义效果；武器/宝物默认值可在总览直接改；未上架精灵框默认收起。
- ℹ️ **关于页**：运行日志旁新增关于（作者 Light / 仓库 https://github.com/imsuperone/xb / 当前版本）。

## v0.7.13
- 🔇 **超管指令全静默**：非超管命中任何超管指令（超管系统/备份/禁言/踢人/清空/维护/测试/超管列表等）均无任何提示（BY DESIGN，详见 AIINFO）；仅版本/检查更新保持所有人可查。

## v0.7.12
- 🧪 **超管测试图片**：`测试图片/测试发图/图片测试` 一次同发 CQ 路径图 + 元组路径图，附绑定状态与文件报告，非超管静默无提示；`_build_chain` 元组字符串路径不再炸成单字。

## v0.7.11
- 🗂️ **图鉴总览分类切换**：武器/宝物/坐骑三按钮切换展示，告别超长页面。
- 🧩 **孤儿精灵可分配**：未上架精灵每卡附带地图下拉 + 分配进图按钮。
- 🐴 **添加坐骑表单窗**：名称/价格/图片一次填完（商城与总览共用）。

## v0.7.10
- 🛒 **修复商城/图鉴空白**：商城解析兼容生产内存 dict 形态（此前 `"[object Object]"` 致 0 件）；精灵改用 `_meta.configured` 判定显式清空（`in` 恒真致预填死代码）；抽奖池加载隔离，失败不再拖垮整个商城页。

## v0.7.9
- 🗂️ **图鉴可用性整改**：商城导出/导入补齐武器商城；孤儿精灵独立成区可编辑删除；自定义宝物通用收藏效果（三处文案统一口径）；宝物增删纳入商城保存流；精灵搜索覆盖道具；进化目标下拉选；精灵形象图字段 + 查看详情出图。

## v0.7.8
- 🗄️ **备份跨进程去重**：文件锁（msvcrt/fcntl）+ busy 预约 + sidecar（30 秒窗口），多进程/热重载并发只出一份；冒烟验证 8 线程并发单文件。
- 🛒 **商城默认回显**：未自定义时编辑器预填内置为起点并标记，告别 0 件；改动保存后转自定义；武器商城接入抽奖池（gacha/weapons 接口 + 一键同步进商城）。
- 🗂️ **类目重组**：删除图鉴可视化工坊；`🛒 商城`（坐骑/武器/精灵道具）与 `📖 图鉴`（精灵图鉴 + 全类目总览）分家。

## v0.7.7
- 🐛 **根治自定义商城/图鉴生产链路不生效**：`store.cfg()` 对 dict 改用 JSON 序列化（原 repr 单引号致引擎 `json.loads` 全失败静默回退内置），自助改价/改图鉴运行时真正生效。
- 🗂️ **图鉴空=内置回退**：精灵 spirits/maps/shop 与坐骑 ride_shop 为空时自动用内置 `spirit_data.py` / RIDES，删默认/删光不再弄坏系统；`spirits/save` 入库整形（9 维属性/地图/道具结构清洗，非标条目丢弃）。
- 🖥️ **图鉴编辑器如实化**：编辑区不再掺内置默认（删了不再“诈尸”）；武器/坐骑/精灵/道具添加补全字段（atk/desc、img、9 维属性）；空态提示内置基数 + 一键载入内置为起点；删最后一件/最后一图二次确认明示回退内置；精灵移除同步清属性孤儿。

## v0.7.6
- 🗑️ **彻底删除帮派武器/女仆坐骑/起名功能**：guild `cmd_weapon` + 购买武器分支 + MENU 行、ride 女仆（RIDES/商城注入/配置键/三档预设/图鉴）、slave `cmd_rename` + 起名分支 + 文本库 10 处常量全删；schema 删 `起名配置` 整节与 6 处废弃键（27 节 741 项）；前端配置分组/数值映射/探针同步清理，用户存量数据不受影响（已购坐骑照常显示）。

## v0.7.5
- 🛡️ **一键平衡真备份**：应用前自动生成 `pre_balance_{mode}_{ts}` 配置快照（备份页快照可一键恢复）+ 全库冷备双保险；模式默认值统一 standard，写入后 `_bump_config_ver` 即时生效；身价校准改走 `group/save_group`（持锁规范、缓存一致、中英键自动翻译，仅补零身价）。
- 🎛️ **指令启用/回复播种**：`指令启用配置` 按采集器播种 236 开关（默认真，行为不变）、`指令回复配置` 补齐 236 场景（默认空=内置回复），僵尸键 `打赏` 改名 `补偿`，后台可禁用任意指令/覆写任意回复。
- 🟡 **休闲档真相修复**：弹窗预选中当前模式（不再永远 standard），三档文案按真实预设值重写，成功 toast 追加数值摘要；补齐预设缺键（赎身/自由间隔、保护时长费用、抽武器花费、打劫银行间隔等），删废弃键（帮派武器/起名/精灵单数初始）。
- ⚡ **热路径与锁**：引擎指令缓存 mtime 探测 10 秒节流、自定义索引指纹懒算（压测 p50 回到 10ms）；`@卡片落盘单任务 + 分群写入；平台动作 8 秒熔断；超管列表 30 秒缓存；rank 解析出锁；反向索引/KNOWN 上限；空投/统计/排行收敛。

## v0.7.4
- ⚡ **@昵称反查反向索引**：`NOTE_NAMES_REV[(gid, 清洗昵称)]` 写时维护，精确命中 O(1)，模糊仅扫本群已清洗键（不清别群、不逐条正则），根治 @ 轰炸下全表扫描延迟。
- 📊 **大屏统计聚合下推**：`analytics/overview` 的 accounts/groups 逐行 `json.loads` 改为 SQL `json_extract/json_each` 聚合 + 持锁短读，输出与旧逻辑逐项等价（含脏串/字符串精灵/空回退语义）。
- 🎁 **空投批量单事务**：`users/airdrop` 从 N 用户 × 3 事务改为缺失行补齐 + 条件更新 + 一次提交（金币钳位/体力下限语义不变，脏缓存用户走老路径，失败整体回滚再降级老路径）。

## v0.7.3
- 💰 **WebUI 排序统一“货币”文案**：用户表排序下拉与表头 `金币` 改为通用 `货币`，改货币名称后不再文案过期。
- 🗄️ **根治双份备份与自动备份时间错乱**：
  - `store.backup_user_data` 新增 `_BACKUP_GEN_LOCK` 串行锁 + 在途标记等待复用，并发/重叠调用只出一份文件（慢速拷贝不持 `_LOCK`，群聊零阻塞）；
  - `main.py` 后台 `xb-auto-backup` 线程按名单例，插件热重载不再累积多 worker 同时 tick；
  - worker 每小时顺带执行一次保留数修剪，间隔未到也能生效。
- 🧹 **保留数量双端生效 + 云端列表分页**：
  - 新增 `backups/prune` 接口与 `webdav.prune_remote_backups`（仅处理 `xbbot_*.db`，留新删旧），上传成功后后台自动跟进修剪，保存备份配置后前端自动触发一次即时生效；
  - WebDAV 远端归档列表 10 份/页分页展示，翻页走缓存不重复请求远端。

## v0.7.2
- 🧩 **根治多群同人昵称串群**：
  - `engines/slave.py` 新增分群昵称表 `NOTE_NAMES_BY_GROUP[(gid, qq)]` 与 `set/get/clear_note_name` 三件套，`fetch_card/uname/exists_user/@反查` 全系优先本群，本群无记录不再回退别群卡片；
  - `main.py` 写路径切分群写入，`bank/sign/ride/guild/adventure/spirit/superadmin` 全引擎展示收敛到分群优先，档案回写只写本群，彻底告别 A 群卡片覆盖 B 群。
- 🗑️ **修复 WebUI 删除用户按钮无效**：
  - `pages/admin/app.js` 弃用 iframe 沙箱下会被拦截的原生 `confirm/prompt`，改用自研 `uiConfirm/uiPrompt` 二次确认（合规则六），加忙态防抖与 POST 失败回退 GET；
  - `userBody` 单一事件委托统一处理保存/导出/清除，杜绝搜索过滤与排序重渲染后绑定丢失；
  - `core/api/users.py:handle_user_clear` 兼容 GET query 取参，清除后同步清理分群昵称内存防幽灵名。

## v0.7.1
- 🕒 **WebDAV 远端归档支持中文日期与上海时区 (UTC+8)**：
  - 在 `core/webdav.py` 中新增 `format_shanghai_time`，将 WebDAV 标准返回的 RFC 1123 HTTP-date（如 `Sun, 06 Sep 2026 05:25:30 GMT`）或 ISO 8601 时间准确换算为中国标准时间（UTC+8 / 上海时区），以标准中文形式展示（如 `2026年09月06日 13:25:30`）；
  - 支持从备份文件名时间戳解析兜底，前后端双向容错，彻底告别原始英文 GMT 时间串。
- 🗑️ **WebDAV 远端备份支持在线直接删除**：
  - 核心模块新增 `delete_remote_file`，遵循 WebDAV RFC 4918 标准发送 HTTP `DELETE` 请求，安全拦截 404/429 并提供友好提示；
  - 后端注册 `backup/webdav/delete` 与 `backups/webdav/delete` 异步接口；
  - WebUI 远端列表各归档右侧新增「🗑️ 删除」按钮，集成危险二次确认弹窗与忙态锁定，删除后自动无感刷新云端归档列表。

## v0.7.0
- 🚀 **版本检测纪元引擎与防缓存强化**：
  - 重构 `core/api/updater.py` 中的 `_parse_version_tuple`，为项目引入版本纪元机制 `(epoch, major, minor, patch)`，将历史 `0.68.xx` 划入旧纪元（epoch=0），确立后续 `0.7.xx`（`0.7.0` ~ `0.7.99`）及 `0.8.xx`、`1.0.xx` 归属新纪元（epoch=1），彻底根除数值比对误报旧版（如误报建议升级至 `0.68.36`）的重大缺陷；
  - 云端版本检测源增加时间戳防 CDN 缓存穿透，优化 GitHub Release 与 Git main 分支双通道择优调度，本地与云端版本检测精准无误。
- 🎯 **模态弹窗 Emoji 全局智能去重**：
  - 在 `pages/admin/app.js` 中新增 `_normalizeModalTitleAndIcon` 核心去重函数，自动提取并剥离 `title` 中的前导 Emoji，杜绝图标与标题堆叠导致的 `🚀 🚀`、`✅ ☁️`、`⚠️ ❌`、`ℹ️ 🩺` 等双重 Emoji 重复问题；
  - 优化版本检测卡片、WebDAV 提示、数据库体检与运行日志等所有弹窗的排版，界面视觉清爽专业。
- ☁️ **WebDAV 远端备份归档与一键快捷热恢复**：
  - 新增 WebDAV RFC 4918 标准 `PROPFIND`（`Depth: 1`）远程文件目录直接解析能力，通过纯标准库 `xml.etree.ElementTree` 安全解析 XML 响应，支持展示云端归档的文件名、大小与修改时间；
  - 在 WebDAV 配置卡片下方新增「☁️ WebDAV 云端备份归档与快捷恢复」面板，支持一键热恢复云端数据库或下载至本地；
  - 恢复流程包含 SQLite 头部签名校验、WAL 检查点截断、连接级原子事务注入与内存缓存清空，热恢复零延迟生效。
- 🛡️ **彻底根除双份重复备份与重复上传**：
  - `store.py` 中为 `backup_user_data` 增加 5 秒全局防抖保护窗口与路径复用机制，彻底解决并发或连续点击产生双份时间戳备份的问题；
  - 为 `backup_user_data` 增加 `auto_upload` 参数，切断手动云端上传流程中触发的重复异步上传，消除双倍云端写入。
- 🎨 **WebUI 按钮视觉反馈与全局 Toast 修复**：
  - 修复 `pages/admin/index.html` 缺失 `#toast` DOM 容器导致的弹窗提示静默退出问题，为 `toast()` 引入自愈机制；
  - 为「测试 WebDAV」与「立即上传云端」按钮增加点击即刻忙态锁定（文字变为 `⏳ 测试连接中...` / `⏳ 正在上传云端...`，并禁用按钮防连击）；
  - 按钮操作结果联动旁边 `<span id="backupCfgMsg">` 实时状态文本与全局居中 `uiAlert` 弹窗。
- 🗄️ **数据库与系统代码全面自检与精简**：
  - 执行 SQLite 完整性检查与碎片整理，全库数据无损健康；
  - 清理废弃的配置快照与冗余代码，保持全代码库极简整洁。

## v0.68.36
- ☁️ **WebDAV 频控与 HTTP 429 根治**：
  - 弃用深层目录遍历 `PROPFIND /` 探测，改为 WebDAV 标准 RFC 4918 `OPTIONS` 轻量连通性嗅探，彻底规避坚果云、Alist、私有 NAS 等云存储服务商的 QPS 频控限制与 HTTP 429 (Too Many Requests)；
  - 增加内存已验证远端目录缓存集合 `_VERIFIED_REMOTE_DIRS`，杜绝重复发起 `MKCOL` 递归创建远程目录；
  - 针对 HTTP 429、401、404、409 等细分状态码提供精准诊断与友好中文指导建议。
- 📦 **SQLite 数据库自包含 100% 完整配置与用户资产**：
  - 在 `save_config()` 中自动将 28 大系统全量配置序列化为 JSON 同步持久化至 SQLite `kv` 表（键名 `sys_config_json`）；
  - 新增 `load_config_from_db()` 支持无 `config.json` 场景下从数据库零损秒级自愈；
  - 每次执行冷备（`backup_user_data` / 定时备份 / 立即备份 / 云端上传）前强制调用 `save_config()`，确保备份出来的任何单一 `.db` 均 100% 自包含所有配置与全部用户资产数据。
- 🎨 **WebUI 现代化体验与交互深度重构**：
  - 移除冗余的「配置快照与一键恢复」面板与前端代码；
  - 将「测试 WebDAV」与「立即上传云端」按钮规范移入「☁️ WebDAV 与自动备份配置」卡片工具栏；
  - 「立即上传云端」全面联动备份列表：在文件列表单击选中具体 `.db` 或 `.json` 备份时可精准定向上传选中的数据库文件，未选中时默认自动冷备最新数据上传；
  - 重构 WebDAV 开关与自动备份开关为统一的高质感 iOS 现代卡片滑块开关（`.switch` + `.slider-toggle`）；
  - 全局模态弹窗 `#appModal` 右上角新增 `✕` 关闭按钮，统一确认按钮为高质感「确定」按钮；
  - 修复版本检测在页面打开时重复检测两次的 Bug，合并为启动后单次静默检测；移除常驻横幅文字，仅在用户主动点击「检查更新」时弹窗展示详情。

## v0.68.35
- 🔌 **AstrBot 插件桥接端点彻底修复 (解决 Plugin bridge endpoint is invalid)**：
  - 彻底根除 `app.js` 原生 bridge 端点校验报错：严禁向 `rawBridge` 传递带有 `?` 或查询参数的 endpoint 字符串；
  - 增强 `getBridge()` 与 `cleanEndpointAndParams` 架构，将 endpoint 严格剥离为纯路径标识，查询参数独立通过原生第 2 参数传递，同时移除前端所有硬编码 `?_t=`；
  - 修复 `backups/list`、`version/check`、`config/get` 等 API 在 AstrBot 原生 WebUI 内部调用时触发的 endpoint 校验异常。
- ⚡ **Quart 异步请求体与全局代理深度适配 (解决 请求体为空或解析失败)**：
  - 全面适配 AstrBot 底层 Quart 异步 Web 框架特性，彻底重构 `core/api/helpers.py` 中的 `get_req_json`；
  - 优先调用 Quart 官方异步方法 `await request.get_json()`，并正确 await Quart 的 `request.json` async property 协程对象，杜绝 `RuntimeWarning: coroutine was never awaited` 导致的空请求体抛错；
  - 增加 `from astrbot.api.web import request as _web_req` 兜底，即便 Web API 路由未显式传递形参也能自动从上下文捕获活跃请求体与查询参数；
  - 统一为 `config/get`、`version/check`、`backups/list` 等核心 GET/POST 响应注入标准 HTTP `Cache-Control: no-cache, no-store, must-revalidate` 禁用缓存响应头，彻底消除浏览器陈旧缓存。

## v0.68.34
- 🚀 **检查更新全方位回显与极速响应**：
  - 检测按钮直接承载状态与结果展示（如 `🟢 已是最新 (v0.68.34)`、`🚀 发现新版 vX.Y.Z`、`⚠️ 重试检测`），不再静默复位为“检查更新”；
  - 工具栏新增即时状态回显标签，无论是否为最新版本均第一时间直观展示检测结果并呼出对比卡片弹窗；
  - 修复模态框内容注入时对 HTML 结构误插入 `<br>` 导致布局破碎的问题；
  - 接入 jsDelivr CDN 高速镜像通道，国内云端版本检测由 16s 大幅降低至 1.5s 极速直达。
- ☁️ **WebDAV 保存读回不一致根治与多层加固**：
  - 修复 AstrBot 原生 Bridge 的 `apiGet` 单形参限制导致 `{_t}` 缓存击穿参数丢失并命中浏览器陈旧缓存的致命缺陷；
  - `get_req_json` 请求体读取全框架加固，覆盖 `json()`、`read()`、`body` 与表单回退，杜绝空请求体假成功；
  - `config/save` 后端响应直接回显持久化后的最新配置，前端双重比对校验，彻底消除“读回不一致”误报。

## v0.68.33
- 🚀 **WebUI 版本检测本地/云端双端提示全面升级**：手动检查更新即使已是最新版本也弹窗并明确展示「当前本地运行版本」与「云端仓库最新版本」双列卡片，配合醒目绿色状态卡片与 Toast 告知双端均处于最新状态；顶栏徽章保持绿色常驻状态并可随时点击查阅版本详情。
- ☁️ **WebDAV 保存与真正投入使用彻底修复**：
  - 根除 AstrBot 重启时默认 schema 物化空值/假值导致用户保存的 `WebDAV备份开关` 与备份配置被覆盖重置的问题，确立持久配置与 DB 镜像绝对优先级；
  - 修复 WebDAV MKCOL 递归创建远端目录缺失尾部斜杠导致 301 重定向变更为 GET 触发 404/409 上传失败的缺陷；
  - 自动规范化 URL 协议前缀（缺失时自动补齐 `https://`）；后端 WebDAV 测试接口与前端输入联动支持动态参数即测即知。

## v0.68.32
- 📖 **交接文档同步现状**：AIREADME 架构树/存储/发布流重写 + 版本履历表；AIINFO 计数对齐实测（41 模块/50 接口/177 DOM/12 Tab）。

## v0.68.31
- ☁️ **WebDAV 配置迁入备份管理**：备份 Tab 新增专属卡片（开关/地址/账号/目录/间隔/保留数），保存后即读回校验，存不住当场暴露；配置页不再重复渲染备份配置。
- 🛟 **全量配置 DB 快照与一键恢复**：每次保存自动留快照（去重，最多 5 份），备份管理一键恢复，改乱丢配置秒回。
- 🔔 **检查更新零静默**：已用 node --check 确认前端无语法死亡；请求加 20s 熔断+按钮忙态+异常形状兜底（旧前端缓存请强制刷新）。

## v0.68.30
- ☁️ **WebDAV 配置三级防丢（文件+DB镜像）**：保存即写透 kv 镜像（含清空语义），启动时文件→DB 逐级回填，AstrBot 怎么洗配置都丢不掉（4 语义全验证）。
- 🔔 **检查更新零静默**：20s 超时熔断+按钮忙态+异常形状兜底，任何情况点击都有提示；失败黄灯可点重试。

## v0.68.29
- ☁️ **WebDAV 保存丢失彻底修复（空值覆盖）**：AstrBot 原生页按 schema 物化空键会覆盖用户值，启动兜底改为“缺键或空值+本地有非空值则回填”，地址/账号重启不再丢失（全链路复现验证）。
- 🔔 **更新检测失败给明确提示**：双通道均失败时返回 `detect_error`，控制台顶栏亮黄灯“更新检测失败”可点击重试，手动检测不再误报“已是最新”。

## v0.68.28
- 🏠 **仓库迁移 `emmfax/xb` → `imsuperone/xb`**：元数据、更新检测、安装文档全量切换。
- 🧩 **接龙奖励改为 20 金币 + 0 魅力并全局锁定**：引擎兜底/schema/种子/三档智能平衡统一；启动时自动纠正历史旧档（400+2 等）残留。
- ☁️ **根治 WebDAV 配置保存丢失**：启动时用本地持久配置补齐 AstrBot 重启过滤掉的节，地址/账号落盘即 survives 重启（已复现验证）。

## v0.68.27
- 🧩 **三对齐审计（库/文件/变量）**：
  - 数据库 5 表字段与全库引用一致，钱包/账户/群组/红包/kv 无漂移；
  - 配置键全覆盖：补 `银行配置.打劫银行间隔/进监狱增加体力` 进 schema（296→298 项），种子配置同步；
  - 中英映射自洽（未知键转义确定性可逆），`data/config.json` 无密钥零QQ可随包发布。

## v0.68.26
- 🛡️ **交付前加固（清空幽灵残留 + 隐私脱敏）**：
  - 清空所有数据同步清空 kv 内存缓存，开关/游戏锁/签到顺序不再幽灵残留；
  - 旧库导入移除开发群号硬编码兜底，改中性 gid，注释示例 QQ 脱敏为占位符。

## v0.68.25
- 📖 **复检回合（零逻辑变更）**：全量复检 verify/压测/前后端对齐全绿；`AIREADME` 架构图同步 `tabs/` 移除现状；8 个用户投诉场景（签到/礼包/接龙重开/转账/菜单）逐条冒烟通过。

## v0.68.24
- 🛡️ **遗留Bug扫尾（打工/槽位/宝物/精灵入口）**：
  - 奴隶槽位/打架格/宝物计数全量改 `_safe_int` + 下限钳位，脏数据不再卡购买与打架；
  - 打工结算跳过开工后新买入奴隶（无本轮工资快照），告别0工资误导；
  - 精灵入口加全局优雅降级护甲，与奴隶系对齐。
- ⚡ **深水区性能（读副本/索引/VACUUM）**：
  - `store` 新增只读副本连接（`query_only` + 独立细锁），`coins_get/recall_get` 高频读不再排队等写锁，16线程×100混合读写零误差；
  - 自定义/禁用/回复覆盖三表统一长度降序索引（配置版本失效重建），每消息三遍遍历合并为一次；
  - 体检 `VACUUM/TRUNCATE` 移出全局锁并 `to_thread` + 60s熔断，WebUI点一次体检不再卡全群。
- 🧹 **删除死亡代码**：`pages/admin/tabs/` 12文件零引用整包移除（verify/scripts零依赖已确认）。

## v0.68.23
- 🛡️ **全库深度体检：3路并发审计修复P0资损×4 + P1×8（零回归）**：
  - **P0-精灵无限循环**：`spirit._exp_need` 配置为0时 `while exp>=0` 恒真卡死，钳位 `>=1`；
  - **P0-红包凭空印钱**：`bank.cmd_recv_red` 降级路径缺空包检查，补 `total<=0` 拦截；
  - **P0-丢弃误删同名**：`spirit.cmd_discard` 按名全量过滤误删多只，改只删首只；
  - **P0-奴隶脏数据崩群**：`slave.handle` 加全局 try/优雅降级，`price=abc` 永不透传；
  - **P1**：转账达上限差额销毁→按credit实扣；超管清空用户导入路径修复；冒险30分钟过期+非法选择严格校验；红包纯口令不再吞数字1/2/3；打劫TOCTOU原子划转；接龙加入边界统一 `>=30s`；私聊5秒限频。
- ⚡ **热路径提速与加载优化**：
  - `main.handle` 引擎字典模块级单例，名片后台写库复用2线程池（防高频群线程爆炸）；
  - `store.group` fetchall快照后锁外解析+双检回填，千人群解析不再阻塞全插件；
  - `router` 守卫缓存上限5000+批量复用TTL 0.3s→2s；
  - `backup/list` 不再触发自动备份（只留守护线程），根除WebUI点开雪崩。
- 🧪 **新增 `scripts/stress_24h.py` 24h等效压测**：4800 ops群聊风暴（p50 11ms/p95 41ms零泄漏）+ 80路WebAPI风暴 + 超时自愈/资金守恒断言。

## v0.68.22
- 🛡️ **彻底根治签到/新手礼包 `database is locked` 与接龙卡死（高并发事务串行化加固）**：
  - **根除接龙 `NameError: owner/now` 重开炸弹**：修复 `ent._check_single_game` 未定义变量导致【开始接龙】永远报“已有进行中”、【结束接龙】报“没有进行中”的死锁假象，现 30 秒超时/开局者重开秒级自愈；
  - **全引擎裸调 `_DB` 持锁串行化**：`bank/superadmin/slave/ride/guild/adventure/spirit/sign` 及 `main` 超管列表全部裸读/裸提交收口至 `with ST._LOCK` + `ST._safe_commit()`，根除跨线程 `database is locked` 与 `cannot rollback`；
  - **新手礼包单事务原子化**：`sign.cmd_newbie` 由 5 次提交合并为 `txn_coins_acct` 一次提交，与签到抢锁冲突归零；
  - **备份全程非阻塞化**：`store.backup_user_data` 仅短持锁做 checkpoint，备份经独立读连接执行，不再长持 `_LOCK` 数秒阻塞消息分发，群聊响应恢复毫秒级；
  - **前台异常优雅降级**：`router` 拦截底层 DB 异常统一转为“系统繁忙，请稍后重试~”，群聊永不暴露原始数据库错误。

## v0.68.21
- ⚡ **彻底根治 56 秒 Event Loop 阻塞（`Event loop lag detected: 56.067s`）与卡死**：
  - **解耦主事件循环与自动备份执行链路**：将原先潜伏在主消息分发路径（`_dispatch`）中的 `maybe_auto_backup()` 彻底剔除，改为在插件初始化阶段启动独立后台守护线程（`xb-auto-backup`）定期检测与执行，主事件循环分发消息耗时恢复至 0ms 纯内存处理；
  - **名片变更与数据库写入全面异步化**：在主消息接收头部提取群名片与昵称时，内存字典缓存瞬时同步（0ms），将 `ST.register_name` 与 `slave.save(gid)` 底层写库剥离至后台线程执行，主事件循环 0 磁盘 I/O、0 锁等待；
  - **WebDAV 与版本检测网络请求全面 `asyncio.to_thread` 异步化**：将 `handle_webdav_test`、`handle_webdav_backup_now`、`handle_version_check` 等 Web API 接口全面封装为异步线程池执行，坚决杜绝因网络延迟、DNS 寻址或握手挂起造成主循环假死；
  - **WebDAV 网络超时与异常诊断深度优化**：PROPFIND 探测超时缩短至 8 秒，PUT 上传超时优化至 25 秒，并细化网络超时与拒绝访问的中文错误诊断。
- 📦 **新增备份自动保留策略（默认保留最新 30 份数据）**：
  - **自动清理超期备份 `clean_old_backups`**：在 `备份配置` 中新增 `保留备份数量`（默认 30 条），每次生成新冷备时自动扫描备份目录，超出保留份数时按时间戳自动清理最旧的 `.db` 备份文件并自动回收清理空日期目录；
  - **可配置化与热生效**：可在 WebUI 控制台或配置文件中按需自定义保留份数（如 10、30、50），填 0 或负数自动兜底为 30。
- ☁️ **WebUI 控制台新增 WebDAV 测试与一键云备份按钮**：
  - 在「备份管理」Tab 工具栏新增【☁️ 测试 WebDAV】与【☁️ 立即上传云端】快捷按钮，并补充清晰的操作状态 Toast 提示反馈；
  - 后端补齐注册 Web API 路由（`/backup/webdav/test`、`/backup/webdav/upload`、`/backups/webdav/*`），打通前端 Web 控制台直连。

## v0.68.20
- 🛡️ **彻底根治并发事务冲突与 `cannot rollback - no transaction is active` 错误**：
  - **切除 `_ensure_db()` 跨线程无锁回滚炸弹**：深入定位发现旧版 `_ensure_db()` 在无锁状态下盲目检查 `in_transaction` 并调用 `rollback()`，当用户快速连续发送「签到」与「领取新手礼包」时，后一线程瞬间强行回滚了前一线程正在执行的活跃事务，导致 SQLite 抛出 `cannot rollback - no transaction is active` 且引发全库死锁；新版彻底切除该破坏性逻辑，保障并发事务安全；
  - **全量底层写入安全降级与杜绝前台报错**：将 `coins_add`、`acct_save`、`save_group`、`txn_coins_acct`、`flush_all` 等方法的原始 `raise` 移除，发生瞬时数据库冲突时自动执行 `_safe_rollback()` 并安全降级返回兜底值，彻底消除聊天群中弹出「处理指令时出现异常」崩溃提示；
  - **签到顺序 GC 纳入全局互斥锁保护**：修复 `sign.py` 在首签清理 7 天前旧键时直接裸调 `_DB.commit()` 的竞争隐患，全面收口至 `with ST._LOCK: ST._safe_commit()`。
- ☁️ **全系自动备份新增 WebDAV 云端自动备份系统**：
  - **纯标准库零外部依赖**：基于 Python 标准库 `urllib.request` 深度自研 WebDAV 客户端，原生支持坚果云、Alist、InfiniCloud、Nextcloud、群晖/威联通 NAS 等任意标准 WebDAV 服务，全平台环境即开即用；
  - **异步静默上传**：每次自动或手动触发备份生成 `.db` 文件后，后台自动唤醒独立 Daemon 线程执行云端同步与目录自动创建（`MKCOL`），零阻塞主聊天消息响应；
  - **超管指令与 Web 控制台深度联动**：超管发送【测试webdav】或【webdav测试】可即时检验云端连通性与账号权限，并在管理大屏提供一键测试与立即云备份。
- 🧩 **接龙死锁解脱与自由退出/重置优化**：
  - **新增【重置接龙】指令**：任何群成员发送【重置接龙】或【结束接龙】，立即全量清理当前残留接龙会话与全部状态键，随时可无缝新开；
  - **开局者离线与脏数据瞬时自愈**：当群内无有效开局者残留，或距上次有效互动超过 30 秒时，发送【开始接龙】立即秒级重开，不再报“已有进行中游戏”。

## v0.68.19
- 🛡️ **彻底根治 `database is locked` 数据库死锁缺陷**：
  - **切除 `_COMMIT_PENDING` 脏事务滞后悬挂**：彻底废弃原先 20 次操作或 100ms 批量提交的滞后逻辑，SQLite 开启事务后未即时 commit 会长期持有排他文件锁；改用即时安全提交 `_safe_commit()` 与异常严格回滚 `_safe_rollback()`，彻底消除残留事务锁；
  - **惰性连接自愈检测 `in_transaction`**：在 `_ensure_db()` 句柄检查中加入活跃事务检测，若存在上一线程遗留的未提交脏事务，自动执行 `rollback()` 自愈释放文件锁，彻底杜绝进程锁死假死；
  - **SQLite 连接与 Pragma busy_timeout 延长至 30 秒**：将 `sqlite3.connect` 超时由 10.0s 增至 30.0s，`PRAGMA busy_timeout` 由 10000ms 增至 30000ms，为高并发读写提供充裕排队缓冲；
  - **修复 `coins_get` 与 `rank_batch` 跨线程无锁并发调用引起的 `SQLITE_MISUSE`**：所有访问底层连接 `_DB` 的查询与备份操作 100% 纳入线程互斥锁 `_LOCK` 保护；
  - **后台备份解耦无阻塞改造**：将备份检查中的 `PRAGMA wal_checkpoint(TRUNCATE)` 替换为非阻塞式 `PRAGMA wal_checkpoint(PASSIVE)`，备份全程不阻塞并发消息处理。
- ⏱️ **接龙及全系娱乐互动游戏 30 秒无响应自动结束机制**：
  - **全系 30 秒超时治理**：接龙、急转弯、猜字谜、猜数字、二四点、问答全系娱乐游戏会话超时时间由 180 秒缩减至 30 秒；
  - **接龙 30 秒无人作答自动释放**：自开局或上一位玩家有效作答起，若 30 秒内无人互动，下一条互动指令（如「加入接龙」、「当前接龙」）或玩家作答时瞬间自动释放游戏位，并提示【接龙超过30秒无人作答，已自动结束~】，彻底告别游戏卡死。

## v0.68.18
- ⚡ **彻底切除 RPC 网络调用，消除 10 秒卡顿与消息队列阻塞**：
  - **彻底移除 `call_action("get_login_info")`**：在 OneBot/NapCat 协议端中，`call_action` 在 WebSocket 异常或无返回时会触发 10 秒（10000ms）默认超时阻塞，导致整个消息接收链路被串行积压 10 秒；新版全面切除所有主动 RPC 网络探测，改为纯同步（0ms）从 AstrBot Event 事件上下文原生属性提取 Bot UIN，完全杜绝网络挂起；
- 🧩 **重构接龙状态机与消除孤儿锁假死**：
  - **修复未开局时「加入接龙」误报“您是开局者”的逻辑漏洞**：彻底修复旧版 `if not owner or str(owner) == str(qq)` 在无游戏进行时因 `not owner` 恒成立而错误回复开局者并误导群员的严重 Bug；无游戏时精准回复友好指引；
  - **开局者重新开局自愈与 60 秒无互动自动重置**：当开局者本人再次发送【开始接龙】或距上一次有效作答超过 60 秒时，系统自动清空历史残存并瞬间开局，彻底解决“已有进行中的接龙游戏”死锁闭环；
  - **新增【当前接龙】/【查接龙】提示指令**：任何时候发送【当前接龙】、【查接龙】、【接龙词】，机器人即时提醒当前出题词语与需接龙的尾字，便于群员接龙；
  - **加入即时提醒词语与尾字**：开局者或已加入者发送【加入接龙】时，自动提示当前词语与尾字，体验大幅优化；
  - **深度孤儿键清理机制**：`_clean_expired_game` 增加对孤儿残留 key 的无锁扫描清洗，确保异常重启后不会遗留孤儿锁。

## v0.68.17
- ⚡ **消息全链路毫秒级极速响应优化（彻底消除 0.x 秒延迟）**：
  - **移除阻塞式 RPC 网络调用**：重构 `main.py` 中的 `_ensure_bot_uin`，将原先在消息分发主流程中每次同步 `await bot.call_action("get_login_info")`（每次耗时 200~800ms 网络往返）彻底重构为同步极速属性提取（0ms），未获取时转入异步后台任务并发拉取，彻底消除主消息处理链路上的网络 RPC 延迟；
  - **群成员昵称极速缓存与锁开销消除**：在 `main.py` 中优化昵称登记逻辑，仅当群成员名片发生变化时才调用 `register_name`，针对群聊持续发言的同一成员实现 0 内存锁开销直接放行；
  - **测试探测模块靶向隔离**：将 `handle_test_probes` 的动态导入与异步检测严格限定在 `测试testxb` 指令命中时才执行，普通群聊消息 0 损耗直达业务处理引擎；
  - **纯文本消息链极速直出**：优化 `core/platform.py` 中的 `_build_chain`，针对无 CQ 图片的普通纯文本与接龙响应（99% 场景）跳过正则表达式与 URL 解码，纳秒级直接生成 Plain 组件返回；
- 🧩 **接龙奖励严格锁定 50 金币与 0 魅力（彻底杜绝 400金币+2魅力 旧配置覆盖）**：
  - 核心逻辑层 `engines/ent.py` 强制锁定接龙成功奖励为 50 金币、0 魅力，不再增加魅力值，文案纯粹呈现 `→ {词语} 奖励50金币`；
  - 同步修正运行时配置 `data/plugin_data/astrbot_plugin_xbbot/config.json` 中的历史遗留数值，确保任何环境下均严格遵循 50 金币、0 魅力。

## v0.68.16
- ⚡ **接龙与群聊消息响应性能万倍提升**：
  - **内存 KV 高速缓存层**：在 `store.py` 中引入线程安全的内存全局缓存 `_KV_CACHE` 与预热加载，`recall_get` 彻底告别磁盘 SQLite 读锁争抢，读取速度从毫秒级磁盘 I/O 提升至纳秒级内存查找（10,000 倍加速）；
  - **杜绝群聊“接不上哦”轰炸塞爆发送队列**：修复 `engines/ent.py` 中接龙进行时对本局玩家在群里的任意 2~6 字正常日常闲聊（如“哈哈”、“在吗”、“好难啊”）全部误当成接龙词并频繁回复“接不上哦！”导致 QQ 协议端发送队列阻塞风控、引发全群严重卡顿延迟的致命问题；首字不符上词尾字时智能静默放行，零打扰群聊；
  - **游戏分支精准分发**：重构 `_play()`，无活跃游戏时 0.001 ms 直接退出，活跃游戏按类型单一命中执行，消减每次普通群消息盲目查询 6 种不同游戏状态的无效数据库 I/O 开销；
  - **词库与自动备份节流**：接龙内置词库与自定义词库预编译缓存，配置未变更时不重复解析；自动备份检测引入 60 秒时间戳内存节流，不再在每条普通群消息中反复检查磁盘文件；
  - **全链路响应时间压降至 1 毫秒内**：接龙与普通群消息路由响应时间由 1253 ms 降至 0.59 ms，极速丝滑。

## v0.68.15
- 🛡️ **全库防假死与静默吞没异常重构**：修复 `core/router.py` 与 `main.py` 在业务引擎执行异常时静默丢弃导致机器人已读不回、假死无响应的重大缺陷；新增异常错误堆栈自动持久化记录（写入 `xb.log`），对明确匹配系统指令的请求杜绝静默失败，自动返回友好故障提示；
- 🔄 **底层数据库全局惰性自愈重连**：全面重构 `store.py`，将 `_ensure_db()` 扩展至钱包、账户、群组、红包与排行榜全套核心方法，确保在模块热重载或数据库句柄重置后 100% 自动自愈重连，杜绝 `NoneType has no attribute 'execute'`；
- 🧩 **接龙与会话游戏死锁及异常时间戳彻底治理**：彻底重构 `engines/ent.py` 超时判定逻辑，支持 `start_ts <= 0` 脏数据与超过 180 秒超时的自动瞬间清扫；接龙退出与超时清空全量关联状态键，并于 `handle` 入口统一执行过期检测；
- 🔍 **奴隶系统前缀截胡治理**：优化 `engines/slave.py` 的【查询】分发逻辑，对系统级查询指令（如查询维护、查询坐骑、查询精灵、查询帮派、查询冒险等）智能放行或直接转发至对应引擎，杜绝误当作群成员昵称而拦截报错；
- 📋 **个人档案兜底加固**：加固 `sign.py` 的 `cmd_personal` 备用展示，打通昵称索引映射，杜绝档案退化。

## v0.68.14
- 🧩 **接龙奖励参数全面归一为 50 金币**：按用户要求，将所有智能数值平衡模式（标准模式、休闲高福利模式、硬核紧缩模式）以及默认配置、运行时配置与 Schema 中的接龙单次成功奖励统一设定为 50 金币，魅力 0，实现各模式下接龙玩法的统一预期体验。

## v0.68.13
- 🧩 **接龙与会话游戏孤儿锁自动回收**：彻底修复 `engines/ent.py` 中因历史残留或脏数据导致 `_clean_expired_game` 无法自动回收超时锁而陷入死锁的问题；新增超过 60 秒无互动自动重启与重开保护，并修复 `store.py` 中 `recall_set` / `recall_get` 惰性数据库自动初始化与异常兜底，杜绝无响应现象；
- 🔍 **AstrBot 原生 At 组件全链路识别与【查询】命令体验升级**：修复 `core/platform.py` 中未兼容 AstrBot 3.4+ 对象型 At 组件导致 @QQ 丢失的问题；【查询】指令未带参数时自动展示发送者本人的完整档案；【查询 昵称】支持括号清洗与双向模糊匹配；【查询更新】智能联动云端版本检测；
- 🚀 **全员可用更新状态检测**：将【更新】、【查询更新】、【检查更新】、【小白更新】等只读查询放行至全员可用，不再误判权限或被娱乐接龙误劫持，随时直观查看当前版本与云端更新。

## v0.68.12
- 📋 **全量个人档案永不退化保障**：彻底修复 `engines/slave.py` 中 `cmd_myinfo` 因历史数据中个别字段类型异常触发未捕获错误导致路由静默降级为 `sign.py` 7行简版信息的问题，增加高鲁棒性数值转换与顶层全量档案保护；`sign.py` 同步委派并对齐全量档案排版，确保无论任何情况下【我的信息】均始终呈现包含资产、存款、身价、体力、魅力、奖券、主人、连签、武器、宝物、奴隶的高清完整档案；
- 🔍 **智能多源昵称反查与自动建档查询**：全面增强【查询 @QQ】、【查询 @昵称】与【查询 昵称】的目标解析能力，打通群实时昵称字典（`NOTE_NAMES`）与群档案反查；被查成员若尚未操作过奴隶玩法，系统将自动进行合规建档并展示其当前档案，彻底消除“格式：查询 @QQ”误报与“Ta还没加入本群游戏”拦截；
- 🛡️ **鲁棒性防御强化**：对历史档案中遗留的非数字、格式混杂的连签与经验数据进行优雅自愈容错，提升多端长时间高频运行稳定性。

## v0.68.11
- 🗺️ **文字冒险系统重构与全数字作答**：秘境扩充至 14 大地图（新增：海底神殿、赛博迷城、幽冥古刹、沙海龙窟、时间回廊），沉浸式 300+ 字长篇剧情与专属事件；选项全面改为 `[1]` / `[2]` / `[3]` 数字格式，冒险中直接回复纯数字 `1`、`2`、`3` 即可极速推进；
- ⛓️ **奴隶系统税收全面移除**：彻底删除买卖奴隶交易税与打工税金扣减，原主人全额获取转卖身价，打工扣减工资后纯利润 100% 归主人所有；全面清理后端计算、文案、Schema、预设及 WebUI 界面税率相关输入框；
- 🧩 **接龙娱乐防刷与数值再平衡**：大幅调降接龙单次奖励至 15 金币、0 魅力，杜绝高额收益刷屏；引入重复相同词拦截、已用词池排重与 2 秒冷却，彻底封堵自循环秒刷漏洞；
- ⚖️ **体力与魅力宏观通胀治理**：科学调优抽奖与签到产出基数，单抽体力降至 10 点、魅力 5 点；签到体力调整为 20~40 点、魅力 5~10 点；冒险消耗体力提升至 10 点，买魅力价格平衡至 50 金币/点；
- 🔍 **全员版本查询与零依赖解析**：重构 `superadmin.py` 版本查询机制，采用零依赖逐行解析，开放全员发送【小白版本】/【版本】直查当前版本，杜绝旧版本信息回退。

## v0.68.10
- 🎰 **抽奖奖励文案纯中文化（彻底告别 English 标签）**：修复 `engines/sign.py` 中中奖奖励直接将内部变量 `stamina` 与 `charm` 输出给玩家的体验缺陷，全面改为标准中文【体力】与【魅力】，连抽与单抽文案 100% 纯中文对齐；
- 🌐 **WebUI 日志端点通信重构与未找到路由彻底修复**：修复 `app.js` 中 `callApi` 误将查询参数直接拼接至 endpoint 路径中导致 AstrBot 框架路由匹配失败并报错“未找到该路由”的问题，严格分离路径名与参数对象；后端路由 `/{PLUGIN_ID}/logs` 等全面支持 `["GET", "POST"]` 双向握手；
- 🎨 **WebUI 界面文案与提示纯净化**：移除终端中所有暴露内部重启机制的引导文案，恢复为沉浸式简洁交互；刷新失败提供专业反馈，成功时精准提示。

## v0.68.9
- 🛡️ **WebUI 日志系统无损导出与弹窗修复（彻底杜绝 404 页面）**：日志「导出」全面采用浏览器端纯内存 `Blob` 结合沙箱穿透下载（`triggerDownload`），彻底解决直接通过 `a.href` 触发浏览器顶层导航导致 AstrBot 拦截渲染“404 WebUI 文件缺失”页面的严重缺陷；当接口响应或数据缓存为空时提供清晰的降级与错误反馈；
- ⚡ **日志控制栏高响应性交互体验升级**：「刷新」按钮点击后立即呈现“正在拉取最新日志…”反馈与成功提示，彻底解决此前因接口未重启或异常时无响应无反馈的痛点；优化「复制」逻辑，严格绑定 `copyToClipboard` 双模复制机制；
- 🔄 **接口通信双向降级与未重启指引**：日志加载与清空逻辑增强容错保护，当检测到后端路由尚未加载时，在控制台终端内主动提示“若刚更新了插件代码，请完全重启一次 AstrBot 以加载新增路由”，体验友好丝滑。

## v0.68.8
- 📜 **WebUI 独立插件运行日志面板**：在管理控制台末位新增「📜 运行日志」独立控制面板，实时展示插件启动、指令分发、超管操作与系统排障日志；
- 🛡️ **严格安全与存储保护（2MB滚动/尾部倒序抓取）**：日志物理存储锁定于 `data/logs/xb.log` 并强制限制在 2MB 以内，超额自动轮转归档，杜绝日志膨胀刷爆磁盘；读取采用尾部分块截断机制，单次请求上限硬限制为 1000 行，禁止全量大文件读入内存；
- 🖥️ **暗黑终端极客交互**：支持日志级别实时过滤（INFO / WARN / ERROR）、关键字即时搜索、3秒智能轮询（切页自动静默防浪费）、自动滚屏、一键复制、清空二次确认与日志文件一键导出。

## v0.68.7
- 📨 **帮派邀请指令路由与文案语法修正**：修复 `engines/guild.py` 中因命令前缀判定写错为 `"guild_invite"` 导致玩家发送【帮派邀请 @QQ】无法响应的死命令缺陷，现全面支持【帮派邀请 @QQ】及【帮派邀请 QQ】；修正帮战提示文案中缺失“没有”的语病（“您还没有创建或加入任何帮派”）；
- 🗺️ **精灵与文字冒险系统文案与指令边界对齐**：精灵菜单指令明确标注为【⚔️ 精灵冒险 地点】，避免与独立文字冒险系统的【冒险 地点】指令混淆冲突；冒险系统菜单精简，并增加对【结束冒险】各类括号后缀变体（如 `结束冒险(消耗复活币)`）的完全兼容；
- ⚡ **奴隶折磨智能目标推导**：优化【折磨奴隶】指令交互，当未附带目标 QQ 时，若当前仅拥有 1 个奴隶则自动聚焦该奴隶执行折磨，无奴隶时精准提示先去买下奴隶，拥有多个奴隶时清晰提示输入目标，大幅提升移动端简写交互体验。

## v0.68.6
- ⛓️ **奴隶自身赎身快捷兼容 (`cmd_ransom` -> `cmd_freedom`)**：优化赎身指令，当被奴役玩家发送【赎身】或【赎身 @自己】未带特定目标时，系统智能识别为“为自己赎身”，自动触发赎身换取自由逻辑，避免提示“你到底想赎哪个奴隶”的体验缺陷；
- 🐉 **精灵对战目标截断与 CQ 码深度修复**：修复当使用【挑战 QQ】（2字前缀）时原切片错误按 4 字截断导致目标 QQ 前两位被削掉的致命缺陷；增加对 `@QQ` 与 CQ 码 `[CQ:at,qq=...]` 的自动解析支持；
- 👥 **管理台定向空投参数绑定类型对齐**：修复管理控制台对特定群进行福利空投时，SQL 参数传递为字符串而非 SQLite INTEGER 类型的潜在类型转换隐患。

## v0.68.5
- 💾 **数据库热恢复机制重构 (Online SQLite Backup)**：重构 WebUI 备份恢复 API，改用 SQLite 原生热备份引擎在线加载数据，彻底解决 Windows 系统下因文件被占用 (`WinError 32`) 导致的恢复失败与文件锁冲突，并在恢复后立即刷新内存缓存，实现无感秒级实时热生效，免重启插件；
- ⏳ **会话类游戏防卡死与超时自愈 (180s Auto-Expire)**：针对答题、急转弯、猜字谜、猜数、二四点等互动游戏全面加入 180 秒超时自愈机制，超时后自动释放单群独占锁，防止因玩家中途退群或遗忘作答导致整个群聊游戏系统被永久卡死；
- 🛡️ **私聊系统敏感词过滤优先级修正**：修正私聊交互中黑名单敏感词判定滞后于普通打招呼关键词（如“你好/晚安”）的逻辑漏洞，确保涉敏消息在任何情况下均第一优先级被安全拦截。

## v0.68.4
- 🏦 **银行取款结息逻辑重大修复**：修复全额取款时因判定失误导致利息被清零、以及部分取款时利息误从自身本金扣除的严重逻辑缺陷，取款本金与派发利息清晰隔离入账；优化保释指令支持“保释自己”快捷交互；
- 👮 **超管清空指令全向修复**：修复超管执行 `清空体力` 与 `清空魅力` 时因键名未映射返回“未知操作”的缺陷，全面支持含 `@QQ` 与 CQ 码的目标解析；
- 🐉 **精灵系统进化种族值与属性对齐**：修复升级进化与使用进化液进化时未继承新进化体种族白值与属性的重大问题，进化后完整获得高阶精灵属性；
- 🐎 **欢迎坐骑冷启动全群漏判修复**：重构坐骑欢迎内存集合初始化机制，首次按需扫描底层库，避免历史已有欢迎坐骑的群聊在重启后被漏判过滤；
- 🗺️ **冒险系统选项全局污染修复**：移除在提示分支时对全局选项列表的就地洗牌操作，保持选项 1/2/3 逻辑规范与预期一致，增加排行榜昵称支持；
- 🏰 **帮派福利每日防刷风控强化**：为帮派每日福利加入严格的每日幂等防护，杜绝无间隔无限重复领取金币的安全隐患；
- ⚔️ **奴隶决斗零除边界防御与前缀补全**：战斗力计算增加零除防御；完整支持 `武器升星` 与 `宝物升阶` 完整 4 字前缀指令。

## v0.68.3
- 🛡️ **开源数据防泄漏严格审计**：插件源码库已彻底剔除所有本地运行产生的测试数据库、用户群档案与备份文件，`.gitignore` 与 GitHub Actions 构建流双层拦截，杜绝私有数据泄露风险；
- 💾 **标准统一数据库命名规范（xb.db）**：存储层核心数据库由 `nuli_slave.db` 全量重构对齐为标准名称 `xb.db`，并提供对历史命名数据库的全自动无损平滑迁移能力；
- 🔒 **AstrBot 外部保险箱持久化（StarTools.get_data_dir）**：持久化数据全部存放在 AstrBot 外部管理的 `data/plugin_data/astrbot_plugin_xbbot/` 独立保险箱，彻底免疫重装、删除或升级时引发的任何数据丢失；
- 🏷️ **全库版本号绝对一致性同步**：所有核心代码、接口与管理控制台全量同步对齐至版本 0.68.3。
